// ─────────────────────────────────────────────────────────
// SECURITY CHECKLIST — 배포 전 전부 체크하지 못하면 배포 금지
// ─────────────────────────────────────────────────────────
// [ ] .env가 .gitignore에 있는가?
// [ ] .env.example만 커밋되었는가?
// [ ] 시크릿 키에 NEXT_PUBLIC_ 접두사가 없는가?
// [ ] 프로덕션 빌드에서 Gemma 호출이 /api/gemma 프록시를 경유하는가?
// [ ] grep -rE "AIza|sk-|Bearer " .next/static/ 결과가 비어 있는가?
// [ ] Gemma 응답이 Zod로 검증되며 실패 시 재시도 1회 후 에러 UI를 띄우는가?
// [ ] mathjs 평가가 safeMath 인스턴스로만 실행되는가?
// [ ] safeParseFn의 노드 타입 화이트리스트를 통과하지 않으면 거부되는가?
// [ ] 이미지 업로드 시 EXIF가 제거되고 리사이징되는가?
// [ ] 이미지의 매직 바이트가 검사되는가?
// [ ] IndexedDB에 원본 이미지나 PII가 저장되지 않는가?
// [ ] CSP 헤더가 설정되어 있고 connect-src가 self로 제한되는가?
// [ ] 프로덕션 빌드에 소스맵이 포함되지 않는가?
// [ ] Rate limit이 프록시에서 작동하는가?
// [ ] 일일 호출 상한이 설정되어 있는가?
// ─────────────────────────────────────────────────────────

export const SECURITY_VERSION = '1.0.0';
